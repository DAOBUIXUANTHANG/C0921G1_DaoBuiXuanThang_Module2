package ss22_structural_design_pattern.thuc_hanh.trien_khai_proxy;

public interface Calculator {
    double sum(double fist, double second);
    double sub(double fist, double second);
    double multi(double fist, double second);
    double div(double fist, double second);
}
